name: headlinehub_models
description: Shared models of the headlinehub apps
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  equatable: ^2.0.7
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.14
  json_serializable: ^6.9.3
  mocktail: ^1.0.4
  test: ^1.25.8
  very_good_analysis: ^6.0.0

# Add the new language model
flutter:
  assets:
    - lib/src/models/language.dart
