name: headlinehub_api
description:  API for the HeadlineHub app and dashboard
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.1.0
  in_memory_headlines_client:
    path: ../../packages/headlines_clients/in_memory_headlines_client
  json_annotation: ^4.8.1
  shelf_cors_headers: ^0.1.5

dev_dependencies:
  mocktail: ^1.0.3
  test: ^1.25.5
  very_good_analysis: ^5.1.0
